{{template "base" .}}

{{define "content" }}
<div class="container">
  <div class="row">
    <div class="column">
      <h1>This is about page</h1>
      <p>This is a edit of about page</p>

      <p>This came from template: {{index .StringMap "test"}}</p>

      <p>
        {{if ne (index .StringMap "remote_ip") ""}}
        Your remote Ip address is {{index .StringMap "remote_ip"}}
        {{else}}
        I don't kmow your ip address yrt. Visit the <a href="/">Home page</a> so
        i can set it
        {{ end }}
      </p>
    </div>
  </div>
</div>
{{ end }}
